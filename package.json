{
  "name": "daloyjs-large-fakerestapi",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.10.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "vercel-build": "pnpm typecheck && pnpm build",
    "dev": "tsx watch scripts/serve.ts",
    "start": "tsx scripts/serve.ts",
    "smoke": "tsx scripts/smoke.ts",
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@daloyjs/core": "^1.3.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "yaml": "^2.9.0"
  }
}
